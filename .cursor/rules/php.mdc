---
description: PHP coding standards and conventions.
globs: **/*.php
---
# PHP コーディング規約

## 必須ルール
- 開始タグは`<?php`を使用（短縮タグ`<?`は使用しない）
- ファイル末尾の終了タグ`?>`は省略（推奨）
- **関数名・変数名**: スネークケース（`my_function_name`）
- **クラス名**: パスカルケース（`MyClassName`）
- 文字列はシングルクォートを優先（変数展開が必要な場合のみダブルクォート）
- **インデントは4スペース**
